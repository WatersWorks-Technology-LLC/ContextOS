# ADR-003: Context is compiled per task

**Status:** Accepted

A permanent universal summary cannot optimize every future task. The context compiler derives a task-conditioned working set from persistent memory for each turn.
